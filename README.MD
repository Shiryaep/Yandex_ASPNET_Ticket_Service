# ASP.NET Core Web API

Это веб‑API на базе ASP.NET Core для бронирования билетов на мероприятие.

# Table of Contents
1. [Требования к запуску](#requirements)
2. [Клонирование репозитория](#clone)
3. [Настройка базы данных](#setupdb)
4. [Запуск приложения](#start)
5. [Проверка работоспособности](#workcheck)
6. [Фильтрация и пагинация событий](#pagination)
7. [Модели приложения](#models)
8. [Особенности приложения](#features)
9. [Запуск тестов](#tests)
10. [Формат ошибок](#errors)
11. [Безопасность и права](#auth)
12. [Получение токена и настройка JWT](#jwt)
12. [Docker](#docker)

<a name="requirements" id="requirements"></a>
## Требования

Перед запуском убедитесь, что у вас установлены:

- [.NET SDK 10.0 или выше](https://dotnet.microsoft.com/download)
- [Visual Studio 2026](https://visualstudio.microsoft.com/) или [Visual Studio Code](https://code.visualstudio.com/)
- [PostgreSQL 15 или выше](https://www.postgresql.org/download/)
- [Docker Desktop](https://www.docker.com/products/docker-desktop/)

<a name="clone" id="clone"></a>
## Клонирование репозитория

1. Откройте терминал или командную строку.
2. Выполните команду для клонирования репозитория и перехода в директорию решения:

   ```bash
   git clone https://github.com/Shiryaep/Yandex_ASPNET_Ticket_Service.git
   cd Yandex_ASPNET_Ticket_Service
   ```
3. Выполните команду для восстановления зависимостей (из корневой директории, где находится файл решения `.sln`):

    ```bash
    dotnet restore
    ```
4. Соберите проект с помощью команды:

    ```bash
    dotnet build
    ```

<a name="setupdb" id="setupdb"></a>
## Настройка базы данных

Приложение использует PostgreSQL в качестве основной базы данных. Строка подключения настраивается в файле `appsettings.json` (или `appsettings.Development.json` для среды разработки).

Пример строки подключения:

```json
"ConnectionStrings": {
  "DefaultConnection": "Host=localhost;Port=5432;Database=TicketServiceDb;Username=postgres;Password=your_password"
}
```

Замените `your_password` на пароль пользователя PostgreSQL. Убедитесь, что база данных `TicketServiceDb` существует, либо оставьте пустой — приложение автоматически создаст её при первом запуске (см. ниже).

### Управление схемой через миграции EF Core

Схема базы данных управляется с помощью миграций Entity Framework Core. Это позволяет безопасно применять изменения структуры базы данных, сохраняя историю миграций и обеспечивая согласованность между различными окружениями.

#### Создание новой миграции

После изменения моделей или конфигураций Entity Framework выполните команду для создания миграции:

```bash
dotnet ef migrations add <MigrationName> --project Infrastructure --startup-project Presentation --output-dir Migrations
```

- Замените `<MigrationName>` на описательное имя миграции (например, `AddNewColumnToEvents`). Миграция будет создана в директории `Migrations` проекта `Infrastructure`.
- Не меняйте аргументы `--project` и `--startup-project`
- Запускайте команду в папке проекта, для которого нужно добавить миграцию. НЕ запускайте команду в корне проекта!

#### Применение миграций к базе данных

Чтобы применить все `pending` миграции к базе данных, выполните:

```bash
dotnet ef database update --project Infrastructure --startup-project Presentation
```

Эта команда обновит схему базы данных до последней миграции. При первом запуске она создаст все таблицы, определённые в миграциях.

#### Откат миграции

Если необходимо откатить последнюю применённую миграцию, используйте:

```bash
dotnet ef database update <PreviousMigrationName> --project Infrastructure --startup-project Presentation
```

Для удаления миграции после ее отката, используйте:

```bash
dotnet ef migrations remove --project Infrastructure --startup-project Presentation
```

> **Примечание:** Убедитесь, что у вас установлен глобальный инструмент EF Core: `dotnet tool install --global dotnet-ef`. Если инструмент не установлен, выполните установку перед использованием команд.

<a name="start" id="start"></a>
## Запуск приложения
### Способ 1. Через командную строку

Запустите приложение из директории решения сразу после сборки:

```bash
dotnet run --project Presentation
```

Приложение будет доступно по адресу: https://localhost:5062 или http://localhost:7011.

### Способ 2. Через Visual Studio

 - Откройте решение (.sln файл) в Visual Studio.

 - Выберите для запуска проект Presentation
 
 - Нажмите F5 или Ctrl+F5 для запуска.

### Способ 3. Через Visual Studio Code

 - Установите расширение C# от Microsoft.

 - Откройте директорию проекта в VS Code.

 - Используйте встроенный терминал для выполнения команды dotnet run.

<a name="workcheck" id="workcheck"></a>
## Проверка работоспособности

После запуска откройте в браузере:

```bash
https://localhost:7011/swagger
```

Вы должны увидеть интерфейс Swagger UI со списком доступных API‑эндпоинтов и кнопкой `Authorize`.

### Доступные эндпоинты

#### Авторизация (Auth)

    POST /api/Auth/register - зарегистрироваться в приложении

    POST /api/Auth/login - войти в приложение

#### Управление событиями (Events)

    GET /api/events — получить список событий

    GET /api/events/{id} — получить событие по ID

    POST /api/events — создать новое событие

    PUT /api/events/{id} - обновить событие по ID

    DELETE /api/events/{id} - удалить событие по ID

#### Бронирование билетов (Bookings)

    POST /api/events/{id}/book — создать бронирование для указанного события

    GET /api/bookings/{id} — получить информацию о бронировании по его ID

    DELETE /api/bookings/{id} - отменить бронирование по его ID

### Коды ответа бронирования

Эндпоинт `POST /api/events/{id}/book` может возвращать следующие HTTP‑коды:

- `202 Accepted` – бронирование успешно создано (статус `Pending`).
- `409 Conflict` – нет свободных мест (выбрасывается исключение `NoAvailableSeatsException`).
- `400 Bad Request` – неверные параметры запроса.
- `404 Not Found` – событие с указанным `id` не существует.

<a name="pagination" id="pagination"></a>
## Фильтрация и пагинация событий

Эндпоинт `GET /api/events` поддерживает следующие параметры запроса:

```
[String]    title - фильтр по заголовку 
[DateTime]  from  - фильтр по дате начала (события, начинающиеся с этой даты или позже)
[DateTime]  to    - фильтр по дате окончания (события, заканчивающиеся до этой даты или раньше)
[Int]       page  - номер страницы (по умолчанию: 1)
[Int]       pageSize - количество событий на странице (по умолчанию: 10)
```

Ответ содержит список событий в формате:

```json
{
  "items": [ ... ],
  "totalCount": 13,
  "page": 1,
  "pageSize": 10
}
```

<a name="models" id="models"></a>
## Модель Event

Событие представлено сущностью `Event` со следующими полями:

```
[Guid]      Id - Уникальный идентификатор события
[String]    Title - Название события
[String?]   Description - Описание
[DateTime]  StartAt - Дата и время начала
[DateTime?] EndAt - Дата и время окончания
[Int]       TotalSeats - Общее количество мест на событие
[Int?]      AvailableSeats - Количество свободных мест
```

Поля `TotalSeats` и `AvailableSeats` используются для управления доступностью мест:
- `TotalSeats` задаётся при создании события и не изменяется.
- `AvailableSeats` уменьшается на 1 при каждом успешном бронировании (метод `TryReserveSeats`) и увеличивается при отмене брони (метод `ReleaseSeats`).

## Модель Booking и статусы

Бронирование представлено сущностью `Booking` со следующими полями:

```
[Guid]           Id - Уникальный идентификатор бронирования
[Guid]           EventId - Идентификатор события, для которого создано бронирование
[BookingStatus]  Status - Текущий статус бронирования
[DateTime]       CreatedAt - Дата и время создания бронирования
[DateTime?]      ProcessedAt - Дата и время обработки бронирования, заполняется при смене статуса
```

## Модель User

Пользователь представлен сущностью `User` со следующими полями:

```
[Guid]      Id - Уникальный идентификатор пользователя
[String]    Login - Имя пользователя для входа
[String]    PasswordHash - Хэш пользовательского пароля
[UserRoles] Role - Роль пользователя - в данном случае User или Admin
```

### Статусы бронирования (`BookingStatus`)

- **Pending** — бронирование создано и ожидает обработки.
- **Confirmed** — бронирование успешно подтверждено (билет забронирован).
- **Rejected** — бронирование отклонено (например, из‑за отсутствия свободных мест).
- **Cancelled** - бронирование отменено пользователем

При создании бронирование получает статус `Pending`. Фоновая служба `BookingResolverService` периодически переводит его в статус `Confirmed`.

<a name="features" id="features"></a>
## Особенности приложения
### Фоновая обработка бронирований

В приложении работает фоновая служба `BookingResolverService`, которая:

- Каждые 3 секунды опрашивает хранилище на наличие бронирований со статусом `Pending`.
- Для каждого такого бронирования имитирует обработку (задержка 2 секунды), после чего переводит его в статус `Confirmed` и устанавливает поле `ProcessedAt` в текущее время.
- Обновляет бронирование в хранилище.

Служба реализована как `BackgroundService` и запускается автоматически при старте приложения.

### Примитивы синхронизации

Для защиты от конкурентных проблем использованы следующие механизмы:

1. **SemaphoreSlim в BookingService** – гарантирует, что только один поток одновременно выполняет критическую секцию в `CreateBookingAsync` (проверка доступности мест, уменьшение `AvailableSeats`, сохранение брони). Это предотвращает овербукинг (продажу одних и тех же мест нескольким пользователям).

2. **SemaphoreSlim в BookingResolverService** – защищает обновление хранилища бронирований при параллельной обработке нескольких `Pending`‑бронирований. Семафор ограничивает одновременный доступ к операциям `Confirm()`/`Reject()` и обновлению хранилища.

Использование `SemaphoreSlim` вместо `lock` обусловлено асинхронной природой методов (`await` внутри критической секции).

### Архитектурные особенности

1. Проект разделен на четыре слоя в соответствии с Clean Architecture + тесты

- Domain — доменные сущности, value objects, доменные исключения. Не зависит ни от чего внешнего.
- Application — use cases, сервисы, интерфейсы портов (репозитории, шлюзы), DTO. Зависит только от Domain.
- Infrastructure — реализации портов: репозитории, DbContext, внешние клиенты. Зависит от Application и Domain.
- Presentation — контроллеры/Minimal API эндпоинты, HTTP-маппинг, регистрация зависимостей. Зависит от Application и Infrastructure.
- Tests - Unit, Интеграционные и E2E тесты, которые зависят только от необходимых слоев.

2. Регистрация зависимостей происходит через вызов extension методов, что позволяет сделать Program.cs компактнее.

## Пример сценария использования

1. **Создание события**  
   ```http
   POST /api/events
   Content-Type: application/json

   {
     "id": "12345678-1234-1234-1234-123456789abc",
     "title": "Linkin Park",
     "description": "Концерт Linkin Park в Яндекс Музыке",
     "startAt": "2026-03-22T10:30:00",
     "endAt": "2026-03-22T12:00:00",
     "totalSeats": 10
   }
   ```
   Ответ содержит созданное событие с присвоенным `Id`.

2. **Получение списка событий с фильтрацией**  
   ```http
   GET /api/events?title=Linkin&from=2026-03-21&page=1&pageSize=8
   ```

3. **Создание бронирования**  
   ```http
   POST /api/events/{eventId}/book
   ```
   Ответ:
   - Код `202 Accepted`
   - Заголовок `Location` содержит URL для отслеживания статуса бронирования (например, `/bookings/12345678-1234-1234-1234-123456789abc`)
   - Тело ответа содержит данные созданного бронирования со статусом `Pending`.

4. **Отслеживание статуса бронирования**  
   ```http
   GET /api/bookings/{bookingId}
   ```
   Пока бронирование обрабатывается, статус будет `Pending`. Через несколько секунд повторный запрос вернёт статус `Confirmed` и заполненное поле `ProcessedAt`.

5. **Удаление события**
    ```http
    DELETE /api/events/{eventId}
    ```

## Пример сценария с овербукингом

Допустим, событие имеет `TotalSeats = 5`. Одновременно 20 пользователей отправляют запросы на бронирование. Без синхронизации могло бы создаться 20 броней, хотя мест всего 5. Благодаря `SemaphoreSlim` в `BookingService`:

- Запросы обрабатываются последовательно внутри критической секции.
- Первые 5 запросов успешно резервируют места (`AvailableSeats` уменьшается с 5 до 0).
- Последующие 15 запросов получают ответ `409 Conflict` (исключение `NoAvailableSeatsException`).
- В результате ровно 5 броней получают статус `Pending`, а затем `Confirmed` фоновой службой.

Это гарантирует, что количество проданных билетов никогда не превысит `TotalSeats`.

<a name="tests" id="tests"></a>
## Запуск тестов

В проекте присутствует набор **Unit**, **Integration** и **E2E** тестов, которые покрывают основные сценарии работы контроллеров, сервисов и хранилищ.

Для запуска всех тестов одновременно выполните команду из директории решения:

```bash
dotnet test
```

### Unit тесты
В проекте присутствует набор **Unit** тестов, расположенных в директории `Tests.UnitTests`.

### Интеграционные тесты

Помимо Unit тестов, проект содержит интеграционные тесты, расположенные в директории `Tests.IntegrationTests`. Эти тесты проверяют взаимодействие с реальной базой данных PostgreSQL, запускаемой в Docker-контейнере с помощью библиотеки Testcontainers.

### E2E тесты

Помимо Unit и Интеграционных тестов, проект содержит End-to-End тесты, расположенные в директории `Tests.E2ETests`. Эти тесты проверяют взаимодействие с HttpClient и работу не только реальной базы данных PostgreSQL, но и всего пайплайна обработки http запроса.

**Требования для запуска интеграционных и E2E тестов:**
- Установленный и запущенный Docker Desktop (или Docker Engine) на вашей машине.

Интеграционные тесты автоматически разворачивают временный контейнер PostgreSQL, применяют миграции (через `Migrate()`) и выполняют тесты, после чего контейнер удаляется. Это позволяет проверить корректность работы репозиториев и сервисов в условиях, максимально приближенных к production.

> **Важно:** Убедитесь, что Docker запущен перед выполнением интеграционных и E2E тестов, иначе тесты завершатся с ошибкой.

<a name="errors" id="errors"></a>
## Формат ошибок

API использует стандартный формат ошибок [Problem Details (RFC 7807)](https://datatracker.ietf.org/doc/html/rfc7807). В случае ошибки клиент получит ответ с соответствующим HTTP‑кодом и JSON‑объектом, содержащим поля `type`, `title`, `status`, `detail` и, при необходимости, `instance`.

Пример ответа с ошибкой 404:

```json
{
  "type": "https://tools.ietf.org/html/rfc7231#section-6.5.4",
  "title": "Not Found",
  "status": 404,
  "detail": "Event with id [12345678-1234-1234-1234-123456789abc] not found",
  "instance": "/events/12345678-1234-1234-1234-123456789abc"
}
```


<a name="auth" id="auth"></a>
## Безопасность и права

В проекте реализован эндпоинт для регистрации и входа пользователя. На данный момент есть две роли - User и Admin. В соответствии с этим разграничением некоторые узлы API доступны только определенным категориям пользователей:

**Доступно всем**
```http
   GET /api/events — получить список событий
   GET /api/events/{id} — получить событие по ID

   POST /api/Auth/register - зарегистрироваться в приложении
   POST /api/Auth/login - войти в приложение
```

**Доступно пользователям**
```http
   POST /api/events/{id}/book — создать бронирование для указанного события
   GET /api/bookings/{id} — получить информацию о бронировании по его ID
   DELETE /api/bookings/{id} - отменить бронирование по его ID
```


**Доступно только администраторам**
```http
   POST /api/events — создать новое событие
   PUT /api/events/{id} - обновить событие по ID
   DELETE /api/events/{id} - удалить событие по ID
```

### При несанкционированном доступе
- При попытке доступа к любым API узлам без авторизации в категориях "Доступно пользователям" и "Доступно только администраторам" будет возвращена ошибка 401 - Unauthorized
- При попытке доступа к админским узлам с правами пользователя будет возвращена ошибка 403 - Forbidden

<a name="jwt" id="jwt"></a>
## Получение токена и настройка JWT

В проекте используется JWT для получения и проверки токена. 

### Получение токена в Swagger

1. Запустите приложение
2. Отправьте запрос на `/api/Auth/Register` со своими данными - так вы зарегистрируетесь в приложении. Сделать это надо единожды - во второй раз будет возвращена ошибка 409 - Conflict, так как пользователь будет уже добавлен.
3. Отправьте запрос на `/api/Auth/Login` со своими данными - в ответ вы получите действующий токен в виде длинной строки с символами.
4. Нажмите на кнопку `Authorize` и введите `Bearer ВАШ-СКОПИРОВАННЫЙ-ТОКЕН` и нажмите `Authorize` - вы получите доступ к API со своими правами

### Настройка JWT

Для настройки JWT в приложении вам необходимо заменить значения в файле `appsettings.json`, а именно:
- В секции Jwt
```
    Key - ваш ключ безопасности для подписи токена. 
    Issuer - сервис, который выдает токены.
    Audience - сервси, для которого выдается токен.
    Lifetime - время в минутах, в течение которого токен актуален.
```
**ВНИМАНИЕ** 
- В Production окружении необходимо использовать длинный и сложный ключ!
- Нельзя коммитить Production ключ, т.е. нельзя комитить изменения в файл `appsettings.json`.
- Держите настоящий файл в безопасном месте, не передавайте его по сети. В репозитории файл присутствует только для примера.

<a name="docker" id="docker"></a>
## Docker

Для запуска приложения вам потребуется докер - о нем уже много раз упоминалось выше. Вам нужно собрать и запустить контейнер с базой данных, чтобы приложение могло подключиться к ней на старте, иначе вы получите ошибку "Невозможно подключиться к серверу"
1. Скопируйте файл `.envexample` в корень проекта и переименуйте его в просто `.env`.
2. Замените в файле все строки начинающиеся с `___` на ваши значения. Например `POSTGRES_DB=___Name of your db like "mydatabase"` замените на `POSTGRES_DB=mydatabase`. Не берите пароли и названия в кавычки!
3. В корне проекта откройте консоль и выполните `docker-compose up -d`. Контейнер автоматически соберется, скачает нужные образы, подцепит переменные из `.env` файла и запустит базу данных.
4. Можно запускать приложение

> **Совет:** Если вы уже поднимали контейнеры, то у вас, скорее всего, остались файлы от предыдущей конфигурации. Если вы будете менять конфигурацию, то используйте команду `docker compose down -v --remove-orphans; docker compose up -d`, чтобы сначала очистить все хвосты, а потом уже поднимать новые контейнеры.